"""Interfaz gráfica del taxímetro (Fase 3, US-09), con tkinter.

Cada pantalla es una clase en su propio módulo; `app.App` es la ventana que
las muestra de una en una. Del dominio solo se usa `ServicioTaximetro`
(`tests/test_estructura.py` lo comprueba).
"""
